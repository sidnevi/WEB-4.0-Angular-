# Taiga UI

Current production-like branch of the prototype.

Scope:
- Taiga UI typography
- Taiga UI palette for surrounding demo UI
- Taiga UI / Angular animation handoff where a matching animation exists
- Notification card keeps its product UI exception exactly as documented

Future rule:
Changes requested for "Taiga UI" must be made only inside `versions/taiga-ui/`
unless the user explicitly asks to sync both versions.
